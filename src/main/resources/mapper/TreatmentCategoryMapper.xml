<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.TreatmentCategoryDao">

    <!--카테고리 목록 조회-->
    <select id="selectVisibleCategories" resultType="com.example.demo.entity.TreatmentCategory">
        SELECT category_id, category_name, is_visible, created_at, updated_at 
        FROM treatment_category 
        WHERE is_visible = TRUE
        ORDER BY category_id 
    </select>

    <!--카테고리 단일 조회-->
    <select id="selectVisibleCategoryById" resultType="com.example.demo.entity.TreatmentCategory">
        SELECT category_id, category_name, is_visible, created_at, updated_at 
        FROM treatment_category 
        WHERE category_id = #{categoryId} AND is_visible = TRUE
    </select>

</mapper>