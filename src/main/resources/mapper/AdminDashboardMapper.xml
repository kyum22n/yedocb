<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.AdminDashboardDao">

    <!--오늘 예약 수 조회-->
    <select id="selectTodayReservationCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM reservation
        WHERE reservation_date = CURRENT_DATE
    </select>

    <!--대기 예약 수 조회-->
    <select id="selectPendingReservationCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM reservation
        WHERE reservation_status = 'PENDING'
    </select>

    <!--오늘 상담 수 조회-->
    <select id="selectTodayConsultationCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM consultation
        WHERE created_at::DATE = CURRENT_DATE
    </select>

    <!--답변 대기 문의 수 조회-->
    <select id="selectWaitingInquiryCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM inquiry
        WHERE inquiry_status = 'WAITING'
    </select>

    <!--PMS 연동 실패 수 조회-->
    <select id="selectPmsFailedCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM reservation
        WHERE pms_sync_status = 'FAILED'
    </select>

    <!--최근 예약 목록 조회-->
    <select id="selectRecentReservations" resultType="com.example.demo.dto.response.reservation.AdminReservationResponseDto">
        SELECT reservation_id, member_id, treatment_id, admin_id, reservation_date, reservation_time, reservation_status, member_memo, admin_memo, pms_sync_status, created_at, updated_at
        FROM reservation
        ORDER BY created_at DESC, reservation_id DESC
        LIMIT 5
    </select>

    <!--최근 문의 목록 조회-->
    <select id="selectRecentInquiries" resultType="com.example.demo.dto.response.inquiry.AdminInquiryResponseDto">
        SELECT inquiry_id, member_id, inquiry_type, title, content, inquiry_status, created_at, updated_at
        FROM inquiry
        ORDER BY created_at DESC, inquiry_id DESC
        LIMIT 5
    </select>

</mapper>
