<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.AdminInquiryDao">

    <!--문의 목록 조회-->
    <select id="selectAllInquiries" resultType="com.example.demo.entity.Inquiry">
        SELECT inquiry_id, u_id, inquiry_type, title, content, inquiry_status, created_at, updated_at
        FROM inquiry
        ORDER BY inquiry_id
    </select>

    <!--회원별 문의 목록 조회-->
    <select id="selectInquiriesByUId" resultType="com.example.demo.entity.Inquiry">
        SELECT inquiry_id, u_id, inquiry_type, title, content, inquiry_status, created_at, updated_at
        FROM inquiry
        WHERE u_id = #{uId}
        ORDER BY inquiry_id
    </select>

    <!--문의 유형별 목록 조회-->
    <select id="selectInquiriesByType" resultType="com.example.demo.entity.Inquiry">
        SELECT inquiry_id, u_id, inquiry_type, title, content, inquiry_status, created_at, updated_at
        FROM inquiry
        WHERE inquiry_type = #{inquiryType}
        ORDER BY inquiry_id
    </select>

    <!--문의 상태별 목록 조회-->
    <select id="selectInquiriesByStatus" resultType="com.example.demo.entity.Inquiry">
        SELECT inquiry_id, u_id, inquiry_type, title, content, inquiry_status, created_at, updated_at
        FROM inquiry
        WHERE inquiry_status = #{inquiryStatus}
        ORDER BY inquiry_id
    </select>

    <!--문의 상세 조회-->
    <select id="selectInquiryById" resultType="com.example.demo.entity.Inquiry">
        SELECT inquiry_id, u_id, inquiry_type, title, content, inquiry_status, created_at, updated_at
        FROM inquiry
        WHERE inquiry_id = #{inquiryId}
    </select>

    <!--문의 상태 수정-->
    <update id="updateInquiryStatus" parameterType="com.example.demo.entity.Inquiry">
        UPDATE inquiry
        SET inquiry_status = #{inquiryStatus},
            updated_at = NOW()
        WHERE inquiry_id = #{inquiryId}
    </update>

    <!--문의 삭제-->
    <delete id="deleteInquiry">
        DELETE FROM inquiry
        WHERE inquiry_id = #{inquiryId}
    </delete>

    <!--답변 등록-->
    <insert id="insertInquiryAnswer"
            parameterType="com.example.demo.entity.InquiryAnswer"
            useGeneratedKeys="true"
            keyProperty="answerId">
        INSERT INTO inquiry_answer
            (inquiry_id, admin_id, answer_content, created_at, updated_at)
        VALUES
            (#{inquiryId}, #{adminId}, #{answerContent}, NOW(), NOW())
    </insert>

    <!--문의 답변 조회-->
    <select id="selectAnswerByInquiryId" resultType="com.example.demo.entity.InquiryAnswer">
        SELECT answer_id, inquiry_id, admin_id, answer_content, created_at, updated_at
        FROM inquiry_answer
        WHERE inquiry_id = #{inquiryId}
    </select>

    <!--답변 상세 조회-->
    <select id="selectAnswerById" resultType="com.example.demo.entity.InquiryAnswer">
        SELECT answer_id, inquiry_id, admin_id, answer_content, created_at, updated_at
        FROM inquiry_answer
        WHERE answer_id = #{answerId}
    </select>

    <!--답변 수정-->
    <update id="updateInquiryAnswer" parameterType="com.example.demo.entity.InquiryAnswer">
        UPDATE inquiry_answer
        SET answer_content = #{answerContent},
            updated_at = NOW()
        WHERE answer_id = #{answerId}
    </update>

    <!--답변 삭제-->
    <delete id="deleteInquiryAnswer">
        DELETE FROM inquiry_answer
        WHERE answer_id = #{answerId}
    </delete>

    <!--문의 답변 삭제-->
    <delete id="deleteAnswerByInquiryId">
        DELETE FROM inquiry_answer
        WHERE inquiry_id = #{inquiryId}
    </delete>

</mapper>
