<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.TreatmentDao">

    <!--진료항목 목록 조회-->
    <select id="selectVisibleTreatments" resultType="com.example.demo.entity.Treatment">
        SELECT treatment_id, category_id, treatment_name, description, is_reservable, is_visible, created_at, updated_at
        FROM treatment
        WHERE is_visible = TRUE
        ORDER BY treatment_id 
    </select>

    <!--카테고리별 항목 조회-->
    <select id="selectVisibleTreatmentsByCategoryId" resultType="com.example.demo.entity.Treatment">
        SELECT treatment_id, category_id, treatment_name, description, is_reservable, is_visible, created_at, updated_at
        FROM treatment
        WHERE category_id = #{categoryId} AND is_visible = TRUE
        ORDER BY treatment_id 
    </select>

    <!--항목 단일 조회-->
    <select id="selectVisibleTreatmentById" resultType="com.example.demo.entity.Treatment">
        SELECT treatment_id, category_id, treatment_name, description, is_reservable, is_visible, created_at, updated_at
        FROM treatment
        WHERE treatment_id = #{treatmentId} AND is_visible = TRUE
    </select>

</mapper>