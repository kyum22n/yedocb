<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.NoticeDao">

    <!--노출 공지/이벤트 목록 조회-->
    <select id="selectVisibleNotices" resultType="com.example.demo.entity.Notice">
        SELECT notice_id, title, content, notice_type, image_url, is_visible, start_at, end_at, created_by, created_at, updated_at
        FROM notice
        WHERE is_visible = TRUE
        ORDER BY notice_id DESC
    </select>

    <!--노출 공지/이벤트 유형별 목록 조회-->
    <select id="selectVisibleNoticesByType" resultType="com.example.demo.entity.Notice">
        SELECT notice_id, title, content, notice_type, image_url, is_visible, start_at, end_at, created_by, created_at, updated_at
        FROM notice
        WHERE is_visible = TRUE AND notice_type = #{noticeType}
        ORDER BY notice_id DESC
    </select>

    <!--노출 공지/이벤트 상세 조회-->
    <select id="selectVisibleNoticeById" resultType="com.example.demo.entity.Notice">
        SELECT notice_id, title, content, notice_type, image_url, is_visible, start_at, end_at, created_by, created_at, updated_at
        FROM notice
        WHERE notice_id = #{noticeId} AND is_visible = TRUE
    </select>

</mapper>
